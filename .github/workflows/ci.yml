name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
    # No `edited`: a PR-body edit (e.g. CodeRabbit injecting its walkthrough)
    # must not restart this heavy matrix (#1005). `edited` never advances the
    # head SHA, so for a PR opened against `main` no commit loses its
    # `CI success` check. One exception: a PR retargeted onto `main` from another
    # base also arrives as `edited`, so its head SHA gets no run until the next
    # push — branch protection then holds it as pending (fail-safe, never a
    # false green). The PR-title lint (pr-title job) stays part of the single
    # `CI success` gate and accepts the resulting no-revalidate-on-title-only-
    # edit gap — see that job's NOTE.
    types: [opened, synchronize, reopened]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1

jobs:
  fmt:
    name: rustfmt
    runs-on: ubuntu-latest
    needs: changes
    if: needs.changes.outputs.rust == 'true' || github.event_name != 'pull_request'
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@1.95.0
        with:
          components: rustfmt
      - run: cargo fmt --all --check

  clippy:
    name: clippy
    runs-on: ubuntu-latest
    needs: changes
    if: needs.changes.outputs.rust == 'true' || github.event_name != 'pull_request'
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@1.95.0
        with:
          components: clippy
      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: clippy
          # Restore-only except on `main`: only pushes to `main` write the
          # shared cache. Other runs (PRs, branch dispatches) warm-start from
          # main's entry but don't each save a ~1 GiB copy that would evict the
          # warm baseline once GitHub's per-repo cache store (currently 10 GiB)
          # saturates.
          save-if: ${{ github.ref == 'refs/heads/main' }}
      - run: cargo clippy --workspace --all-targets -- -D warnings
      # `--workspace` unifies features, so `decdn-cli` turns on
      # `decdn-incentive/redb` for everyone and the two configurations the
      # feature split exists to create are never actually compiled: the node's
      # (`buyer-store-core` without `redb`, #1246) and the contracts/voucher
      # consumer's (neither, so redb isn't linked). Both are real — a plain
      # `cargo build -p decdn-node`, or any downstream taking `decdn-incentive`
      # alone — and both break silently under unification. Check them directly.
      - run: cargo clippy -p decdn-incentive --no-default-features -- -D warnings
      - run: >
          cargo clippy -p decdn-incentive --no-default-features
          --features buyer-store-core -- -D warnings
      # A feature that no dev-dependency turns on is invisible to the
      # `--workspace --all-targets` run above: `anvil-e2e` (all 15
      # `crates/e2e/tests/*` + the two gated `decdn-node` tests),
      # `decdn-protocol/public-api-test`, and `decdn-client/public-api-test`. No
      # anvil job ever invokes clippy — settlement builds only the two `--test`
      # targets it names, journeys only `decdn-e2e`'s — so the gated targets sat
      # outside the lint wall every other target obeys and drifted into failing
      # it (#1551).
      #
      # `--all-features` rather than a hand-listed `-p A -p B --features F`: the
      # curated `--test` list at the settlement job below is how
      # `anvil_bringup_shutdown_e2e` rotted, and a package list rots the same
      # way the first time a third crate gains a gated target. The cost is
      # honest — a second feature configuration in this job's
      # `shared-key: clippy` cache — and it buys back the rot risk.
      #
      # Linting here rather than in the anvil jobs keeps the fast feedback on
      # this job's warm cache and off those jobs' hour-scale timeouts. This is a
      # LINT gate, not a build-parity check: it compiles one unified
      # all-features configuration, which is not the per-job feature set either
      # anvil job builds.
      - run: cargo clippy --workspace --all-targets --all-features -- -D warnings

  deny:
    name: cargo deny
    runs-on: ubuntu-latest
    needs: changes
    if: needs.changes.outputs.rust == 'true' || github.event_name != 'pull_request'
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@1.95.0
      # cargo-deny pinned to an exact version so a release can't silently
      # change the check's behavior.
      - uses: taiki-e/install-action@v2.87.16
        with:
          tool: cargo-deny@0.20.2
      # Findings warn (annotations on the run summary) and the job succeeds.
      # A run that checked nothing — deny.toml error, advisory-db fetch
      # failure — fails the job. See cargo_deny_warn.py.
      - run: .github/scripts/cargo-deny-warn.sh

  doc:
    name: cargo doc
    runs-on: ubuntu-latest
    needs: changes
    if: needs.changes.outputs.rust == 'true' || github.event_name != 'pull_request'
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@1.95.0
      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: doc
          # Restore-only except on `main`; only `main` writes it (see clippy job).
          save-if: ${{ github.ref == 'refs/heads/main' }}
      - run: cargo doc --workspace --no-deps --document-private-items
        env:
          RUSTDOCFLAGS: "-D warnings"

  doctest:
    name: doctest
    runs-on: ubuntu-latest
    needs: changes
    if: needs.changes.outputs.rust == 'true' || github.event_name != 'pull_request'
    # REQUIRED gate. `cargo llvm-cov nextest` in the `test` job does NOT execute
    # doctests, so the `compile_fail` guard at crates/node/src/receipt_log.rs
    # (and any future runnable doctest) would never run (#864). This job runs
    # them so a doctest regression blocks the PR.
    #
    # It lives in its own job — not appended to `test` — on purpose. A bare
    # `cargo test --doc` cannot reuse the coverage-instrumented artifacts the
    # `test` job builds (cargo-llvm-cov instruments the workspace via a rustc
    # wrapper; the plain doctest build has a different fingerprint), so sharing
    # that job's target/cache forced a full ~5.5 min dependency-graph rebuild on
    # the critical path. Here the build is plain/non-instrumented with its OWN
    # `shared-key: doctest` cache partition: after `main` warms it, a PR reuses
    # the cached deps and rebuilds only changed workspace crates, AND the whole
    # thing runs in parallel with `test` instead of extending it. Doctest
    # coverage is not the goal (it would need nightly) — execution is.
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@1.95.0
      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: doctest
          # Restore-only except on `main`; only `main` writes it (see clippy job).
          save-if: ${{ github.ref == 'refs/heads/main' }}
      - run: cargo test --doc --workspace

  # NOTE: there is intentionally no separate "MSRV check" job. The workspace
  # MSRV (`rust-version = "1.95.0"`) equals the toolchain pinned in
  # rust-toolchain.toml (1.95.0), which every Rust job here installs — the
  # `packaging` job's toolchain-pin check refuses the two to differ. A
  # `cargo check` on that same toolchain is a strict subset of the `clippy`
  # job's `cargo clippy --workspace --all-targets` (clippy is rustc + extra
  # lints), so it added runner-minutes for zero additional signal. Reintroduce
  # a dedicated job ONLY if the MSRV is ever lowered below the pinned dev
  # toolchain — then install that older floor explicitly and `cargo check`
  # against it.

  public-api:
    name: public API snapshot
    runs-on: ubuntu-latest
    needs: changes
    if: needs.changes.outputs.rust == 'true' || github.event_name != 'pull_request'
    # Guards the exported surface of `decdn-protocol` (issue #304) and of the
    # `decdn-client` SDK (#1150). The
    # `public-api-test` feature-gated test renders the public API from rustdoc
    # JSON and `insta`-snapshots it; an unintended diff fails here and blocks
    # the PR. Runs on the same pinned stable toolchain as everything else (the
    # test sets RUSTC_BOOTSTRAP=1 to emit rustdoc JSON without nightly). Kept a
    # separate job (clean env, no coverage instrumentation / mold) so it never
    # perturbs the required `test` job. Each step runs only the `public_api`
    # target: the `test` job owns every other test in both crates, and running
    # them here beside the rustdoc render starves `decdn-client`'s
    # timing-sensitive scheduler tests.
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@1.95.0
      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: public-api
          # Restore-only except on `main`; only `main` writes it (see clippy job).
          save-if: ${{ github.ref == 'refs/heads/main' }}
      - uses: taiki-e/install-action@v2.87.16
        with:
          tool: cargo-nextest
      - run: cargo nextest run -p decdn-protocol --features public-api-test --test public_api
      - run: cargo nextest run -p decdn-client --features public-api-test --test public_api

  changes:
    name: detect changes
    runs-on: ubuntu-latest
    # Only contents: read — `token: ''` below keeps paths-filter off the
    # listFiles API, so the pull-requests: read scope it needed is gone.
    permissions:
      contents: read
    outputs:
      rust: ${{ steps.filter.outputs.rust }}
      adr: ${{ steps.filter.outputs.adr }}
      solidity: >-
        ${{ steps.filter.outputs.solidity_dependencies == 'true' ||
            steps.solidity_contracts.outputs.solidity == 'true' }}
      markdown: ${{ steps.filter.outputs.markdown }}
      scripts: ${{ steps.filter.outputs.scripts }}
      docker: ${{ steps.filter.outputs.docker }}
    steps:
      - uses: actions/checkout@v7
      - uses: dorny/paths-filter@v4
        id: filter
        with:
          # Detect changes with git, not the pulls/{n}/files API. That endpoint
          # is a hard single point of failure for the whole pipeline: when it
          # 503s, this job dies, every downstream job reports `skipping`, and
          # the `ci-success` gate fails — with nothing having compiled. A
          # GitHub incident did exactly that and no re-run could clear it.
          # `token: ''` is the action's documented switch for this; git keeps
          # working through REST outages.
          #
          # Semantics are unchanged: checkout leaves us on the PR merge commit,
          # so paths-filter's `git diff base..HEAD` yields the same file list
          # the API returned. It self-fetches the base SHA, so no fetch-depth
          # bump is needed. On `push` this job already used git.
          token: ''
          filters: |
            rust:
              - '**/*.rs'
              - '**/Cargo.toml'
              - 'Cargo.lock'
              # The ed25519-vector generator is a standalone workspace; its lock
              # is the parity target for crates/incentive/tests/ed25519_pin_parity.rs
              # (run by the `test` job), so a lock-only bump must trigger rust.
              - 'contracts/test/ed25519-vectors/Cargo.lock'
              - 'rust-toolchain.toml'
              - 'rustfmt.toml'
              - 'deny.toml'
              - '.github/scripts/cargo_deny_warn.py'
              - '.github/scripts/cargo-deny-warn.sh'
              - '.cargo/**'
              # insta snapshot baselines (e.g. the public-API surface snapshot,
              # issue #304). Without this, a PR that hand-edits only a committed
              # *.snap would have rust=false and skip the `public-api` job that
              # validates it — letting a stale/incorrect baseline merge.
              - '**/*.snap'
              - '.github/workflows/ci.yml'
            adr:
              - 'adr/**/*.md'
              - 'adr/Makefile'
              # Guard helpers driven by adr/Makefile (check-anchor-targets), so
              # a change to the guard itself re-runs it against the whole set.
              - 'adr/_build/*.awk'
              # docs/ and root markdown are in ANCHOR_SRCS (they link into ADR
              # anchors), so a change to any of them must re-run the anchor guard.
              - 'docs/**/*.md'
              - 'CONTRIBUTING.md'
              - 'CLAUDE.md'
              - 'CHANGELOG.md'
              - 'README.md'
              - '.github/workflows/ci.yml'
            solidity_dependencies:
              - '.gitmodules'
              - '.github/workflows/ci.yml'
              - '.github/actions/**'
              - '.github/scripts/post-gas-snapshot-comment.sh'
              - '.github/scripts/lcov_to_cobertura.py'
            markdown:
              - '**/*.md'
              - '.markdownlint.yaml'
              - '.github/workflows/ci.yml'
            scripts:
              # Gates `ci-tooling-tests` (the pytest suite under
              # .github/scripts/tests). The helpers and their regression tests
              # live under .github/scripts/; a PR that touches no scripts has
              # nothing for that job to verify.
              - '.github/scripts/**'
              - '.github/workflows/ci.yml'
            docker:
              # Gates `docker-build`. Must live in THIS filter, not the
              # Solidity one below: that step sets
              # `predicate-quantifier: every`, which would require a path to
              # match every rule at once — no file is both Dockerfile and
              # .dockerignore, so the filter could never fire and the job
              # would silently skip on the very PRs it exists to check.
              - 'Dockerfile'
              - '.dockerignore'
              - '.github/workflows/ci.yml'
      - name: Detect Solidity contract changes
        uses: dorny/paths-filter@v4
        id: solidity_contracts
        with:
          token: ''
          # dorny ORs rules by default, so a negation is not subtraction. With
          # `every`, a path must be under contracts/ and outside deployments/.
          # This lives in its own action because the quantifier is action-wide.
          predicate-quantifier: 'every'
          filters: |
            solidity:
              - 'contracts/**'
              # Deployment manifests are recorded artifacts, not contract
              # inputs. Test-chain 31337*.json manifests are also gitignored.
              - '!contracts/deployments/**'

  test:
    name: test
    runs-on: ubuntu-latest
    needs: changes
    if: needs.changes.outputs.rust == 'true' || github.event_name != 'pull_request'
    # The `Run tests` step (instrumented nextest) is the required signal feeding
    # ci-success. The coverage upload after it carries `continue-on-error: true`
    # so a GitHub API failure never blocks a PR.
    # Doc-only PRs skip this job via the `changes` gate; alls-green is
    # configured with `allowed-skips` (see ci-success below) so the skip
    # is accepted as success.
    permissions:
      contents: read
      # `Upload code coverage` posts the Cobertura report to GitHub Code Quality.
      code-quality: write
    # RUSTFLAGS lives at the JOB level (not step level) so the
    # Swatinem/rust-cache step that runs early in this job sees it. That
    # action hashes CARGO_*/RUST_* env vars into its cache key, so this
    # placement gives the `coverage` partition a key that uniquely reflects
    # the build configuration: cache misses cleanly when the flags change,
    # the post-job step then saves the freshly-built target. Step-level
    # scoping was the wrong choice — Swatinem didn't see the env, restored
    # main's old-flag target, cargo invalidated everything via per-artifact
    # fingerprint and rebuilt from scratch, and the post-step skipped the
    # save with "Cache up-to-date" because the (unchanged) key already
    # existed. The new build was thrown away every run; see PR #484 history.
    # Other jobs (clippy/doc) are in separate job blocks and do not
    # inherit this env, so their caches stay warm.
    env:
      # Three link/debug knobs:
      #   - link-arg=-fuse-ld=mold: route the final link of the test
      #     binaries through mold instead of the system ld.
      #   - debuginfo=line-tables-only: emit only the .debug_line DWARF
      #     section, which is what cargo-llvm-cov reads to map counters to
      #     source lines (SF/DA records in lcov.info). Strips subprogram
      #     DIEs and type/variable info. NOT equivalent to `-C debuginfo=1`
      #     (= "limited", which keeps subprogram DIEs); line-tables-only is
      #     strictly tighter.
      #   - split-debuginfo=unpacked: side-file debug info; smaller .o
      #     files, faster relinking. Orthogonal to coverage.
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold -C debuginfo=line-tables-only -C split-debuginfo=unpacked"
      # Incremental compilation files bloat the cache for zero gain in CI
      # (no second compile in the same run); rust-cache's README also
      # recommends disabling it.
      CARGO_INCREMENTAL: "0"
    steps:
      - uses: actions/checkout@v7
      # Reclaim ~20 GB of preinstalled toolchains this job never uses (see the
      # settlement e2e job for the full ENOSPC rationale). This job's load is
      # the instrumented target — a partial rust-cache restore plus a rebuild
      # beside it under `--no-clean` — then the llvm-cov profraw merge, then on
      # main the cache repack. Without the headroom the merge fails with ENOSPC
      # after every test has passed.
      - name: Free disk space
        run: |
          sudo rm -rf /opt/hostedtoolcache/CodeQL \
                      /usr/local/lib/android \
                      /usr/share/dotnet \
                      /opt/ghc \
                      /usr/local/.ghcup
          sudo docker image prune --all --force || true
          df -h /
      - uses: dtolnay/rust-toolchain@1.95.0
        with:
          components: llvm-tools-preview
      # mold is a drop-in replacement for ld that noticeably shortens the
      # link phase of the workspace's instrumented test binaries. Scoped to
      # the test job because that's where the test binaries get linked —
      # other jobs do link small artifacts (build scripts, proc-macro
      # dylibs) but the savings would be marginal. setup-mold pulls a
      # release binary instead of apt, which sidesteps the dpkg-lock and
      # mirror stalls that hang `apt-get install` on hosted runners.
      - name: Install mold linker
        uses: rui314/setup-mold@v1
        # RUSTFLAGS routes the link through mold explicitly via `-fuse-ld=mold`,
        # so mold only needs to be on PATH — don't rewire the system `ld`
        # symlink (the action's default) and perturb unrelated C/C++ links.
        with:
          make-default: false
      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: coverage
          # Restore-only except on `main`; only `main` writes it (see clippy
          # job). This is the largest partition (~1.2 GiB instrumented target),
          # so gating its save is what most relieves the saturated cache store.
          save-if: ${{ github.ref == 'refs/heads/main' }}
      - uses: taiki-e/install-action@v2.87.16
        with:
          tool: cargo-llvm-cov,cargo-nextest

      # REQUIRED step. cargo llvm-cov nextest runs the workspace test suite
      # with LLVM instrumentation; non-zero exit on any test failure fails
      # this job and (via ci-success.needs) blocks the PR. No continue-on-error.
      # sccache is not wired here (matching the prior coverage job); Swatinem
      # /rust-cache with shared-key: coverage covers caching of instrumented
      # build artifacts on its own. Build-flag env (RUSTFLAGS,
      # CARGO_INCREMENTAL) lives at the job level above so rust-cache's
      # cache key reflects them.
      #
      # --no-clean: cargo-llvm-cov runs `cargo clean --package <each>` per
      # invocation by default, which would wipe the instrumented artifacts
      # rust-cache just restored. This flag suppresses that cleanup so the
      # cache restore is actually useful.
      #
      # --cobertura: GitHub Code Quality reads Cobertura XML only.
      - name: Run tests (with coverage instrumentation)
        run: cargo llvm-cov nextest --workspace --cobertura --output-path cobertura.xml --no-tests=pass --no-clean

      # Disk headroom after the profraw merge, logged even when the tests fail,
      # so an ENOSPC regression shows up as a number rather than a guess.
      - name: Report disk usage
        if: always()
        run: df -h /

      # Posts the Rust coverage to GitHub Code Quality, which comments the PR
      # coverage and per-file deltas against `main`. Best-effort: a failure
      # never blocks the PR. The action skips fork PRs by itself (their token
      # is read-only).
      - name: Upload code coverage
        if: hashFiles('cobertura.xml') != ''
        continue-on-error: true
        uses: actions/upload-code-coverage@v1
        with:
          file: cobertura.xml
          language: Rust
          label: code-coverage/cargo-llvm-cov
          fail-on-error: false

      # Doctests are NOT run here. `cargo llvm-cov nextest` does not execute
      # them, and a bare `cargo test --doc` appended to this job cannot reuse
      # the coverage-instrumented artifacts (different rustc fingerprint), so it
      # recompiled the whole dependency graph — ~5.5 min, on the critical path.
      # They now live in the dedicated `doctest` job below, which builds
      # non-instrumented against its own warm cache partition and runs in
      # parallel, off this job's critical path. See that job for the #864
      # rationale.

  # Live anvil-backed on-chain e2e (issue #745). Gated behind the `anvil-e2e`
  # cargo feature so the default `test` job stays fast and needs no Foundry. Split
  # into two parallel jobs — settlement and cross-layer journeys — so their
  # (independent, build-context-distinct) workspace compiles run concurrently
  # instead of back-to-back; the split overlaps the two compiles to cut
  # wall-clock (most on a cold cache) at the cost of a duplicated setup + compile
  # per job. Both need BOTH toolchains: the Rust tests
  # spawn `anvil` and shell out to `forge` (DeployProtocol.s.sol + the
  # mintable-USDC mock). Both run when either Rust or Solidity paths changed;
  # accepted via ci-success allowed-skips otherwise.
  #
  # Build-flag env (RUSTFLAGS/CARGO_INCREMENTAL) is JOB-LEVEL, not step-level, so
  # Swatinem/rust-cache hashes it into the cache key — step-level scoping restores
  # a stale-flag target and rebuilds from scratch (see the `test` job's PR #484
  # note). mold + reduced debuginfo mirror that job: this path links the
  # settlement test / 13 journey binaries / the two bins, and `line-tables-only`
  # keeps panic-backtrace line numbers for flaky-journey triage.
  anvil-e2e-settlement:
    name: anvil e2e (settlement)
    runs-on: ubuntu-latest
    # Bound derived from OBSERVED job duration (~2m on `main`), but floored by the
    # #785 ordering rather than set to a bare multiple of it. The settlement
    # test's own in-test `OVERALL_TIMEOUT` is 13m (it contains ~560s of internal
    # poll budgets + build + deploy, and this PR does not retier it), so a hung
    # test surfaces its clear per-test message only if this job-level kill sits
    # above 13m + the pre-test work (the workspace compile and the hoisted
    # `forge build` below). 15m would leave too little for that pre-test work;
    # 20m clears the 13m ceiling with room and is still ~10x the observed run — a
    # large cut from the old 60m. `forge build` is hoisted to the step below,
    # off the cold path inside the test (#1620).
    timeout-minutes: 20
    needs: changes
    if: >-
      needs.changes.outputs.rust == 'true' ||
      needs.changes.outputs.solidity == 'true' ||
      github.event_name != 'pull_request'
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold -C debuginfo=line-tables-only -C split-debuginfo=unpacked"
      CARGO_INCREMENTAL: "0"
    steps:
      # submodules: recursive — the in-test `forge build` needs the forge-std +
      # openzeppelin-contracts submodules under contracts/lib/.
      - uses: actions/checkout@v7
        with:
          submodules: recursive
      # Reclaim ~20 GB of preinstalled toolchains this job never uses. The
      # GitHub-hosted runner ships ~14 GB free on /; this job compiles the full
      # workspace, shells out to an in-test `forge build`, AND — on main only
      # (save-if below) — repacks target/ for the Rust cache. That combination
      # overflows the default free space, so the post-job cache-save fails with
      # ENOSPC and reds the job even though the e2e test itself passed. Freeing
      # the unused SDKs up front gives the build and the save headroom.
      - name: Free disk space
        run: |
          sudo rm -rf /opt/hostedtoolcache/CodeQL \
                      /usr/local/lib/android \
                      /usr/share/dotnet \
                      /opt/ghc \
                      /usr/local/.ghcup
          sudo docker image prune --all --force || true
          df -h /
      - uses: dtolnay/rust-toolchain@1.95.0
      # mold shortens the link phase of the workspace's test binaries; installed
      # before rust-cache so `-fuse-ld=mold` in RUSTFLAGS resolves at link time.
      # setup-mold pulls a release binary rather than apt, avoiding the
      # dpkg-lock / mirror stalls that hang `apt-get install` on hosted runners.
      - name: Install mold linker
        uses: rui314/setup-mold@v1
        # RUSTFLAGS routes the link through mold explicitly via `-fuse-ld=mold`,
        # so mold only needs to be on PATH — don't rewire the system `ld`
        # symlink (the action's default) and perturb unrelated C/C++ links.
        with:
          make-default: false
      # Installs the pinned Foundry (anvil + forge on PATH) and warms the solc
      # cache. cache-profile: none — the contract build is the hoisted step below.
      - uses: ./.github/actions/foundry-setup
        with:
          cache-profile: none
      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: anvil-e2e-settlement
          save-if: ${{ github.ref == 'refs/heads/main' }}
      - uses: taiki-e/install-action@v2.87.16
        with:
          tool: cargo-nextest
      # Hoist the cold contract compile out of the tests (#1620). Both anvil
      # targets shell out to their own `forge build` (a 180s single-attempt cap
      # with no retry); running it once here keeps that cold build — which on a
      # contended cold cache can approach the cap — off the introducing PR's
      # in-test path, leaving each test's build a warm incremental no-op.
      - name: Build contracts once (hoisted from the tests)
        working-directory: contracts
        run: forge build
      # In-process anvil e2e targets: each spawns anvil + shells out to forge,
      # then drives real node code against the live chain — settlement through
      # `PaymentChannelService`, bring-up through `runtime::run`. Neither needs a
      # daemon/CLI binary on disk (that's why the split can drop the bin build).
      #
      # Both targets are named EXPLICITLY. A bare `-p decdn-node --features
      # anvil-e2e` would drag the whole unit suite in behind the feature flag and
      # duplicate the `test` job, but a single `--test` filter is how
      # `anvil_bringup_shutdown_e2e` silently rotted: it was never built by any
      # job, so it stopped compiling for three days without a red build and was
      # even edited in that state. Any new `anvil_*` target must be added here.
      # The clippy job's `--all-features` run COMPILES every gated target, so
      # that particular rot cannot recur silently — but compiling is not running.
      # A target missing from the list below still never executes.
      - name: Run on-chain anvil e2e
        run: |
          cargo nextest run -p decdn-node --features anvil-e2e \
            --test anvil_settlement_e2e \
            --test anvil_bringup_shutdown_e2e

  anvil-e2e-journeys:
    name: anvil e2e (journeys)
    runs-on: ubuntu-latest
    # Bound derived from OBSERVED job duration, not from a sum of per-test
    # ceilings. That sum-of-ceilings model assumed every test simultaneously
    # burned its full budget — which cannot happen — and went stale four times as
    # journeys landed (#1620 has the history). The job runs in ~6-9m on `main`
    # (the ~9m tail is the cold compile the job also covers, not the tests); 30m
    # is ~3.5x the worst observed run, with headroom for that compile, the
    # daemon+CLI build, and the hoisted `forge build` below.
    #
    # This still honors the #785 ordering — a hung test must surface its own
    # clear per-test message before this opaque job-level kill. The largest
    # per-test `OVERALL_TIMEOUT` is the 10m heavy tier
    # (`crates/e2e/src/timeout.rs`), and each fixture's binding in-test retry
    # ladder — the deploy ladder, now capped at ~240s (`crate::chain`) — is
    # contained by that tier, so a real deploy flake reports as
    # "deploy failed after N attempts" rather than as a generic timeout. 30m
    # clears 10m + the compile + the builds with room to spare.
    timeout-minutes: 30
    needs: changes
    if: >-
      needs.changes.outputs.rust == 'true' ||
      needs.changes.outputs.solidity == 'true' ||
      github.event_name != 'pull_request'
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold -C debuginfo=line-tables-only -C split-debuginfo=unpacked"
      CARGO_INCREMENTAL: "0"
    steps:
      # submodules: recursive — the in-test `forge build` needs the forge-std +
      # openzeppelin-contracts submodules under contracts/lib/.
      - uses: actions/checkout@v7
        with:
          submodules: recursive
      # Reclaim ~20 GB of preinstalled toolchains this job never uses (see the
      # settlement job above for the full ENOSPC rationale).
      - name: Free disk space
        run: |
          sudo rm -rf /opt/hostedtoolcache/CodeQL \
                      /usr/local/lib/android \
                      /usr/share/dotnet \
                      /opt/ghc \
                      /usr/local/.ghcup
          sudo docker image prune --all --force || true
          df -h /
      - uses: dtolnay/rust-toolchain@1.95.0
      # mold shortens the link phase of the journey + bin binaries; installed
      # before rust-cache so `-fuse-ld=mold` in RUSTFLAGS resolves at link time.
      # setup-mold pulls a release binary rather than apt, avoiding the
      # dpkg-lock / mirror stalls that hang `apt-get install` on hosted runners.
      - name: Install mold linker
        uses: rui314/setup-mold@v1
        # RUSTFLAGS routes the link through mold explicitly via `-fuse-ld=mold`,
        # so mold only needs to be on PATH — don't rewire the system `ld`
        # symlink (the action's default) and perturb unrelated C/C++ links.
        with:
          make-default: false
      # Installs the pinned Foundry (anvil + forge on PATH) and warms the solc
      # cache. cache-profile: none — the contract build is the hoisted step below
      # (a `ci`-profile cache would need the fixtures on that same forge profile,
      # else their default-profile build recompiles; left as a follow-up, #1620).
      - uses: ./.github/actions/foundry-setup
        with:
          cache-profile: none
      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: anvil-e2e-journeys
          save-if: ${{ github.ref == 'refs/heads/main' }}
      - uses: taiki-e/install-action@v2.87.16
        with:
          tool: cargo-nextest
      # Hoist the cold contract compile out of the fixtures (#1620). Each
      # `decdn-e2e` fixture shells out to `forge build`; running it once here
      # means only this step pays the cold compile, and every fixture's in-test
      # build is a warm incremental no-op. This keeps the cold build off
      # whichever test nextest schedules first and is what lets the per-test
      # ceilings shrink to the `crates/e2e/src/timeout.rs` tiers — a cold build
      # (CI worst case ~30m) no longer sits inside a per-test `OVERALL_TIMEOUT`.
      - name: Build contracts once (hoisted from the fixtures)
        working-directory: contracts
        run: forge build
      # The cross-layer `decdn-e2e` fixtures spawn the built `decdn-node`
      # binary, and the slash-appeal journey additionally shells out to the
      # user CLI `decdn` (package `decdn-cli`), so both must exist on disk
      # before the journeys run.
      - name: Build daemon + CLI for cross-layer fixtures
        run: cargo build -p decdn-node -p decdn-cli
      # Cross-layer journeys (smoke + G-NODE-04 blacklist compliance, #1031 +
      # G-NODE-05 slash appeal, #1032).
      # --test-threads 2: each journey runs its own anvil + forge deploy +
      # daemon(s); more at once oversubscribes the 4-core runner and turns
      # poll-based assertions flaky, so cap concurrency at two.
      - name: Run cross-layer e2e journeys
        run: cargo nextest run -p decdn-e2e --features anvil-e2e --test-threads 2

  # ----- Solidity contracts ----------------------------------------------
  # The contracts pipeline mirrors the Rust split (one concern per job).
  # All jobs gate on `needs.changes.outputs.solidity` and are accepted
  # via ci-success allowed-skips when no contracts/ paths changed.
  # Foundry is pinned to a concrete release tag (v1.7.1) for reproducibility.

  solidity-fmt:
    name: solidity fmt
    runs-on: ubuntu-latest
    needs: changes
    if: needs.changes.outputs.solidity == 'true' || github.event_name != 'pull_request'
    defaults:
      run:
        working-directory: contracts
    steps:
      - uses: actions/checkout@v7
      - uses: foundry-rs/foundry-toolchain@v1.9.1
        with:
          version: v1.7.1
      - run: forge fmt --check

  # Ed25519 differential-vector freshness guard (issue #1008). The on-chain
  # Ed25519Verifier must stay at least as strict as the ed25519-dalek
  # verify_strict the node runs off-chain (#669); committed test vectors pin
  # that parity. This job catches a stale vector: the generator or its pin
  # changed without re-pasting. The complementary *version-parity* guard — the
  # generator's isolated dalek pin must match the version decdn-incentive
  # resolves — is a Rust test (crates/incentive/tests/ed25519_pin_parity.rs) run
  # by the `test` job, not here.
  ed25519-vectors:
    name: ed25519 vectors
    runs-on: ubuntu-latest
    needs: changes
    # Drift is triggered by a Rust dependency bump (the node's dalek, via
    # Cargo.lock / a workspace Cargo.toml — `rust` filter) or by a change to the
    # generator or the consuming contracts (both under contracts/ — `solidity`
    # filter). Gate on both so either re-runs the check.
    if: >-
      needs.changes.outputs.rust == 'true' || needs.changes.outputs.solidity == 'true' ||
      github.event_name != 'pull_request'
    steps:
      # This job only checks out to regenerate + diff vectors locally; it never
      # pushes, and releases are not automated from CI (no auto-release), so the
      # GITHUB_TOKEN need not persist in .git/config. Dropping it satisfies
      # zizmor's `artipacked` check and keeps the token off disk for later steps.
      - uses: actions/checkout@v7
        with:
          persist-credentials: false
      - uses: dtolnay/rust-toolchain@1.95.0
      - uses: Swatinem/rust-cache@v2
        with:
          # The generator is a standalone workspace, isolated from the root one.
          workspaces: contracts/test/ed25519-vectors
          shared-key: ed25519-vectors
          save-if: ${{ github.ref == 'refs/heads/main' }}
      # forge fmt normalises the regenerated Solidity; no solc/build needed, so
      # the bare toolchain (not ./.github/actions/foundry-setup) is enough.
      - uses: foundry-rs/foundry-toolchain@v1.9.1
        with:
          version: v1.7.1
      # Freshness: regenerating must reproduce the committed vectors.
      - name: Regenerate vectors
        run: cargo run --locked --manifest-path contracts/test/ed25519-vectors/Cargo.toml -- --write
      - name: Format regenerated vectors
        working-directory: contracts
        run: forge fmt test/Ed25519Verifier.t.sol test/CapacityBondRegionE2E.t.sol
      - name: Fail if vectors are stale
        run: |
          set -euo pipefail
          if ! git diff --exit-code -- \
            contracts/test/Ed25519Verifier.t.sol \
            contracts/test/CapacityBondRegionE2E.t.sol; then
            echo "::error::Ed25519 differential vectors are stale vs a fresh dalek run. Regenerate with 'cargo run -- --write' in contracts/test/ed25519-vectors, run 'forge fmt', and commit the result."
            exit 1
          fi

  solidity-build-test:
    name: solidity build+test
    runs-on: ubuntu-latest
    needs: changes
    if: needs.changes.outputs.solidity == 'true' || github.event_name != 'pull_request'
    # pull-requests: write is for the gas-snapshot sticky comment (PR events
    # only; fork PRs skip it — read-only token). The gas snapshot is folded
    # into this job rather than living in a separate `solidity-gas-snapshot`
    # job: `forge snapshot` runs the full test suite to measure gas, so a
    # standalone job re-ran the entire ci-profile fuzz/invariant suite
    # (1024 fuzz runs; 256 invariant runs at depth 50) a second time. Running
    # `forge snapshot` here as the single test invocation gives both the
    # pass/fail gate and the gas diff from one execution.
    permissions:
      contents: read
      pull-requests: write
    defaults:
      run:
        working-directory: contracts
    env:
      # foundry.toml profile.ci raises fuzz to 1024 runs and invariant depth to
      # 50, with traces (verbosity=3) for failed cases.
      FOUNDRY_PROFILE: ci
    steps:
      # submodules: recursive is required — forge-std and openzeppelin-contracts
      # are git submodules under contracts/lib/, and OZ pulls ds-test transitively.
      - uses: actions/checkout@v7
        with:
          submodules: recursive
      - uses: ./.github/actions/foundry-setup
        with:
          cache-profile: ci
      - run: forge --version
      - run: forge build --sizes --deny warnings
      # Single test execution: `forge snapshot --snap` runs the whole suite
      # (same pass/fail gate as `forge test`) AND records gas to a temp file in
      # one pass — no second run of the ci-profile fuzz/invariant suite. We then
      # FAIL the job if the committed baseline is stale, so a contract change
      # that moves gas can't merge until the author regenerates and commits it
      # (the job is path-filtered to `contracts/**`, so it only runs when
      # contracts actually change).
      #
      # The comparison is scoped to DETERMINISTIC entries: lines containing
      # `runs:` (fuzz mean/median, invariant call counts) are stripped from both
      # sides first. Foundry's fuzz μ/~ gas is NOT reproducible across machines
      # even at a fixed seed — the committed values come from a contributor's
      # box and never match a fresh CI run — so gating on them would fail every
      # PR. The ~620 non-fuzz entries ARE byte-stable across machines at the
      # pinned forge + solc version, and every contract has deterministic unit
      # tests, so a real gas movement still trips the gate. `grep -v` output is
      # newline-terminated on both sides, so this is also immune to the
      # trailing-newline difference between forge's writer and editors/hooks.
      # The deterministic diff is captured for the sticky PR comment (posted
      # even on failure, below). `set -euo pipefail` makes a test failure
      # (non-zero `forge snapshot`) abort the job too; re-run locally with -vvvv
      # for full call traces.
      - name: Test + gas snapshot
        id: gas
        run: |
          set -euo pipefail
          if [[ ! -f .gas-snapshot ]]; then
            echo "::error::contracts/.gas-snapshot baseline missing; regenerate via 'FOUNDRY_PROFILE=ci forge snapshot --snap .gas-snapshot'"
            exit 1
          fi
          forge snapshot --snap "${RUNNER_TEMP:-/tmp}/new.gas-snapshot"
          grep -v 'runs:' .gas-snapshot > "${RUNNER_TEMP:-/tmp}/base.det"
          grep -v 'runs:' "${RUNNER_TEMP:-/tmp}/new.gas-snapshot" > "${RUNNER_TEMP:-/tmp}/new.det"
          if ! diff -u "${RUNNER_TEMP:-/tmp}/base.det" "${RUNNER_TEMP:-/tmp}/new.det" > /tmp/gas-diff.txt; then
            echo "::error::contracts/.gas-snapshot is stale vs the contracts in this PR (deterministic gas changed). Regenerate with 'FOUNDRY_PROFILE=ci forge snapshot --snap .gas-snapshot' and commit the result."
            cat /tmp/gas-diff.txt
            exit 1
          fi
      # Structural enforcement that `TestnetFaucet` never leaks into the
      # production audit surface (issue #636, #452). The contract lives at
      # `testnet/TestnetFaucet.sol` and is wired only from
      # `script/TestnetFaucet.s.sol` + the test suite — any reference from
      # `src/` or from any other `script/*.s.sol` fails CI.
      - name: Verify TestnetFaucet stays in testnet boundary
        run: |
          set -euo pipefail
          # Explicit dir-existence check so a future refactor that renames /
          # removes `src/` or `script/` fails the gate loudly instead of
          # silently passing — without it, `grep`'s missing-dir error is
          # swallowed by `2>/dev/null` and `|| true` and the gate degrades
          # to a no-op against the new layout.
          for d in src script; do
            if [[ ! -d "$d" ]]; then
              echo "::error::expected directory contracts/$d to exist for the TestnetFaucet boundary check"
              exit 1
            fi
          done
          # Note: this intentionally matches comment mentions too. If a
          # production file ever needs to *name* TestnetFaucet (e.g., in a
          # doc reference), the right move is to update this gate
          # deliberately, not to soften it.
          leak=$(grep -rnw 'TestnetFaucet' src/ script/ \
                   | grep -v '^script/TestnetFaucet\.s\.sol:' || true)
          if [[ -n "$leak" ]]; then
            echo "::error::TestnetFaucet must not appear outside contracts/testnet/ or contracts/script/TestnetFaucet.s.sol"
            echo "$leak"
            exit 1
          fi
      # Post (or update) the sticky gas-snapshot diff comment. PR-only; skipped
      # on fork PRs (read-only GITHUB_TOKEN would 403). continue-on-error so a
      # transient GitHub API hiccup in this best-effort tooling never blocks the
      # required gate.
      # `if: always() && steps.gas.outcome in (success, failure)`: post the
      # comment WHETHER OR NOT the snapshot gate passed — a stale-gas failure is
      # exactly when the author needs to see which entries drifted (the gate
      # step wrote the unified diff to /tmp/gas-diff.txt before exiting). The
      # `outcome` guard scopes it to runs where the snapshot step actually
      # executed, so a `forge build` failure (snapshot skipped, no diff file)
      # posts nothing. A clean run leaves an empty diff and the script renders
      # "No gas changes vs base." continue-on-error only forgives a failure of
      # THIS step (the API call), not of the prior gate.
      - name: Post gas snapshot comment
        if: >-
          always()
          && (steps.gas.outcome == 'success' || steps.gas.outcome == 'failure')
          && github.event_name == 'pull_request'
          && github.event.pull_request.head.repo.full_name == github.repository
        continue-on-error: true
        env:
          GH_PR_NUMBER: ${{ github.event.pull_request.number }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: ../.github/scripts/post-gas-snapshot-comment.sh /tmp/gas-diff.txt

  solidity-fork-test:
    name: solidity fork test
    runs-on: ubuntu-latest
    needs: changes
    if: needs.changes.outputs.solidity == 'true' || github.event_name != 'pull_request'
    defaults:
      run:
        working-directory: contracts
    env:
      FOUNDRY_PROFILE: fork
      # Live RPC endpoint for the one fork suite, `GenesisBuybackActivation.fork`
      # (issue #1090), which exercises the Arbitrum Sepolia deploy path #1083 takes.
      # `contracts/foundry.toml`'s `arbitrum_sepolia` `[rpc_endpoints]` alias
      # interpolates this var, so the two names change together. That suite forks at
      # LATEST and creates + seeds its own pool, so a plain public (non-archive)
      # endpoint suffices. The secret is absent on fork PRs (secrets are withheld)
      # and in forks that never configured it — the run step below no-ops when it is
      # unset and the suite self-skips on its own, so this job never blocks a PR for
      # a missing secret.
      ARBITRUM_SEPOLIA_RPC_URL: ${{ secrets.ARBITRUM_SEPOLIA_RPC_URL }}
    steps:
      # submodules: recursive — forge-std + openzeppelin-contracts are submodules.
      - uses: actions/checkout@v7
        with:
          submodules: recursive
      - uses: foundry-rs/foundry-toolchain@v1.9.1
        with:
          version: v1.7.1
      # A skipped step yields a SUCCESSFUL job, and the checks UI renders
      # "solidity fork test ✓" identically whether the fork suite passed or never
      # ran at all — telling them apart otherwise means opening the log and noticing
      # an absence. This step always runs and puts the state in the job summary.
      # Deliberately not a failure: fork PRs cannot have secrets by policy, and a
      # hard gate here would turn main red whenever the secret is unavailable.
      - name: Fork RPC configuration
        if: always()
        run: |
          if [ -z "$ARBITRUM_SEPOLIA_RPC_URL" ]; then
            echo "::warning::ARBITRUM_SEPOLIA_RPC_URL is not configured — the fork suite did not run"
            echo "- ⚠️ \`ARBITRUM_SEPOLIA_RPC_URL\` unset — suite skipped" >> "$GITHUB_STEP_SUMMARY"
          else
            echo "- ✅ \`ARBITRUM_SEPOLIA_RPC_URL\` configured" >> "$GITHUB_STEP_SUMMARY"
          fi
      # The suite self-skips when the var is unset, which is what keeps an offline
      # `forge test` green; the `if:` below only avoids burning a runner.
      # `forge test --match-path` exits 0 on a glob that matches nothing, so a rename
      # or a typo would otherwise go green — and, with the summary above, green next
      # to a configured-secret checkmark. The count catches that rename; proving a
      # test body ran is the `-vv` output's job.
      - name: Fork tests (no-op when no fork RPC is set)
        if: env.ARBITRUM_SEPOLIA_RPC_URL != ''
        run: |
          matched=$(ls -1 test/*.fork.t.sol 2>/dev/null | wc -l)
          echo "- fork suites matched by the glob: $matched" >> "$GITHUB_STEP_SUMMARY"
          if [ "$matched" -eq 0 ]; then
            echo "::error::test/*.fork.t.sol matched no files — the fork suites have moved or been renamed"
            exit 1
          fi
          forge test --match-path 'test/*.fork.t.sol' -vv

  solidity-coverage:
    name: solidity coverage
    runs-on: ubuntu-latest
    needs: changes
    if: needs.changes.outputs.solidity == 'true' || github.event_name != 'pull_request'
    permissions:
      contents: read
      # `Upload code coverage` posts the Cobertura report to GitHub Code Quality.
      code-quality: write
    defaults:
      run:
        working-directory: contracts
    env:
      FOUNDRY_PROFILE: coverage
    steps:
      - uses: actions/checkout@v7
        with:
          submodules: recursive
      - uses: ./.github/actions/foundry-setup
        with:
          cache-profile: coverage
      # The coverage profile disables optimizer + via_ir for accurate source
      # mapping. If forge coverage hits "stack too deep" as more contracts
      # land, add --ir-minimum here. --no-match-coverage drops test/ (suites
      # and mocks), so the report measures src/, testnet/ and script/.
      - name: forge coverage
        run: forge coverage --report lcov --report summary --no-match-coverage 'test/'

      # GitHub Code Quality reads Cobertura XML only and forge writes LCOV
      # only. Forge's paths are relative to contracts/; --path-prefix makes
      # them repo-relative so the report lands on the real files. Both steps
      # are best-effort: a failure never blocks the PR. The upload action
      # skips fork PRs by itself (their token is read-only).
      - name: Convert coverage to Cobertura
        continue-on-error: true
        run: python3 ../.github/scripts/lcov_to_cobertura.py lcov.info cobertura.xml --path-prefix contracts

      - name: Upload code coverage
        if: hashFiles('contracts/cobertura.xml') != ''
        continue-on-error: true
        uses: actions/upload-code-coverage@v1
        with:
          # `uses:` steps ignore defaults.run, so the path is workspace-relative.
          file: contracts/cobertura.xml
          language: Solidity
          label: code-coverage/forge
          fail-on-error: false

  solidity-solhint:
    name: solidity solhint
    runs-on: ubuntu-latest
    needs: changes
    if: needs.changes.outputs.solidity == 'true' || github.event_name != 'pull_request'
    defaults:
      run:
        working-directory: contracts
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7.0.0
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: contracts/package-lock.json
      # npm ci (not npm install) enforces the committed lock file so the
      # transitive dependency graph stays pinned, not just solhint itself.
      - run: npm ci --no-audit --no-fund
      - run: npm run lint

  solidity-slither:
    name: solidity slither
    runs-on: ubuntu-latest
    needs: changes
    if: needs.changes.outputs.solidity == 'true' || github.event_name != 'pull_request'
    permissions:
      contents: read
      # GitHub Advanced Security is NOT enabled on decdn/decdn (private
      # repo, no GHAS license), so the SARIF upload step below is
      # commented out. The slither-action's `fail-on: medium` is still
      # the authoritative CI gate (findings ≥ medium fail the job and
      # surface in the run log). Re-enable the upload step + add back
      # `security-events: write` and `actions: read` permissions if
      # GHAS is ever turned on or the repo flips public.
    steps:
      - uses: actions/checkout@v7
        with:
          submodules: recursive
      # cache-profile omitted (none): crytic-compile drives its own `forge
      # build`, so we cache only the solc binaries it downloads, not artifacts.
      - uses: ./.github/actions/foundry-setup
      - name: Run Slither
        id: slither
        # SHA-pinned (v0.4.2). slither-config carries fail_on: medium too;
        # `fail-on` on the action input is the authoritative gate.
        uses: crytic/slither-action@b52cc1cbfee9ca3e8722dd5224299d16c9a6b80f
        with:
          target: contracts/
          slither-config: contracts/slither.config.json
          sarif: results.sarif
          fail-on: medium
      # SARIF upload disabled until GHAS is enabled on this repo (see
      # job-level permissions comment above). Restore by uncommenting
      # the step below and adding `security-events: write` + `actions:
      # read` back to `permissions:`. The slither step still produces
      # results.sarif on disk; only the upload-to-code-scanning is
      # gated.
      # - name: Upload SARIF
      #   if: always() && steps.slither.outputs.sarif != ''
      #   uses: github/codeql-action/upload-sarif@v4.35.5
      #   with:
      #     sarif_file: ${{ steps.slither.outputs.sarif }}
      #     category: slither

  solidity-aderyn:
    name: solidity aderyn
    runs-on: ubuntu-latest
    needs: changes
    if: needs.changes.outputs.solidity == 'true' || github.event_name != 'pull_request'
    steps:
      - uses: actions/checkout@v7
        with:
          submodules: recursive
      # cache-profile omitted (none): aderyn-ci manages its own build, so we
      # cache only the solc binaries, not artifacts.
      - uses: ./.github/actions/foundry-setup
      # The official Cyfrin/aderyn-ci action installs aderyn, runs it, and
      # gates the CI step via fail-on. No SARIF surface (which we don't
      # need anyway — code scanning requires GHAS on private repos), and
      # the action handles its own toolchain lifecycle.
      - name: Aderyn check
        # SHA-pinned (v0.0.10). The action emits a `working-directory`
        # deprecation warning suggesting `root` in aderyn.toml; both work
        # today and the warning is benign — the action still respects
        # the input.
        uses: Cyfrin/aderyn-ci@6c7e313326652c60f82f804b7f3641650334baaf
        with:
          working-directory: contracts
          fail-on: high

  # NOTE: the former standalone `solidity-gas-snapshot` job was folded into
  # `solidity-build-test` (see its "Test + gas snapshot" step). It re-ran the
  # full ci-profile fuzz/invariant suite a second time purely to measure gas;
  # `forge snapshot` already runs that suite, so one invocation now serves both
  # the test gate and the gas diff. Removed from ci-success needs/allowed-skips
  # accordingly.

  pr-title:
    name: PR title
    runs-on: ubuntu-latest
    # Conventional-commit lint on the PR title. Part of the single `CI success`
    # gate (ci-success.needs + allowed-skips below), so a non-conformant title
    # blocks merge without a second required status check. PR-only — skipped on
    # push to main and workflow_dispatch and accepted via allowed-skips. Reads
    # PR metadata only (no PR code is checked out), so plain `pull_request` with
    # a read-only token is safe — the action just hits the GitHub API.
    #
    # NOTE (#1005): this workflow intentionally does NOT trigger on `edited`
    # (see `on.pull_request.types` above), so a PR-body edit can't restart the
    # heavy matrix. Consequence for this check: the title is validated on open
    # and on every push (synchronize), but a title edited to a non-conformant
    # value with no subsequent push is not re-checked before merge. Any later
    # push (or reopen) re-validates. Coupling `edited` back in to close that gap
    # would re-introduce the duplicate-run / stale-green problem #1005 fixed.
    if: github.event_name == 'pull_request'
    permissions:
      pull-requests: read
    steps:
      - uses: amannn/action-semantic-pull-request@v6
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          types: |
            feat
            fix
            docs
            chore
            refactor
            perf
            test
            build
            ci
            revert
          requireScope: false
          subjectPattern: ^(?![A-Z]).+$
          subjectPatternError: |
            The subject "{subject}" found in the pull request title "{title}"
            didn't match the configured pattern. Please ensure that the subject
            doesn't start with an uppercase character.

  markdownlint:
    name: markdownlint
    runs-on: ubuntu-latest
    needs: changes
    # Gated on the `markdown` filter (any **/*.md or the lint config). On a
    # PR that touches no Markdown there is nothing to lint, so the job skips
    # and is accepted via ci-success allowed-skips. Still runs unconditionally
    # on push/dispatch. The `changes` job remains the always-run canary that a
    # skip here can no longer mask.
    if: needs.changes.outputs.markdown == 'true' || github.event_name != 'pull_request'
    steps:
      - uses: actions/checkout@v7
      - uses: DavidAnson/markdownlint-cli2-action@v24
        with:
          globs: |
            **/*.md
            !adr/_build/**
          config: ".markdownlint.yaml"

  adr-book-list:
    name: ADR book check
    runs-on: ubuntu-latest
    needs: changes
    # Runs only when the ADR book changes (adr/**/*.md or adr/Makefile);
    # skips otherwise (accepted via ci-success allowed-skips below).
    # Job id stays `adr-book-list` — referenced by ci-success needs/allowed-skips.
    if: needs.changes.outputs.adr == 'true'
    steps:
      - uses: actions/checkout@v7
      # `make` is preinstalled on ubuntu-latest; every target is pure shell
      # (awk/sed/grep, no pandoc/typst/mermaid) and runs sub-second. Separate
      # steps so a failure points at the specific guard in the checks UI.
      - run: make -C adr check-book-list
      - run: make -C adr check-bare-refs
      - run: make -C adr check-md-anchors
      - run: make -C adr check-anchor-targets
      - run: make -C adr check-no-section-numbers
      - run: make -C adr check-no-history-links

  # Pytest suite for CI helper and path-filter regressions. Non-blocking by design
  # — excluded from ci-success.needs — so a regression in the helper surfaces
  # as a red X (visible in the checks list) without blocking PRs. Lives in its
  # own job so the signal stays distinct from yellow continue-on-error steps
  # in the required `test` job.
  ci-tooling-tests:
    name: CI tooling tests
    runs-on: ubuntu-latest
    needs: changes
    # Gated on the `scripts` filter: this only exercises .github/scripts/, so a
    # PR that touches none has nothing to verify here. Not in ci-success.needs
    # (non-blocking by design), so a skip needs no allowed-skips entry.
    if: needs.changes.outputs.scripts == 'true' || github.event_name != 'pull_request'
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: '3.12'
      - name: Test CI tooling
        run: |
          python3 -m pip install 'pytest==8.*' 'pyyaml==6.*'
          python3 -m pytest .github/scripts/tests -q

  # Guards what `cargo publish` will do on release day. Both checks exist
  # because a crates.io version can never be replaced: by the time the real
  # publish runs, the release is signed and its dependency crates are already
  # uploaded, so a packaging error found there has no clean recovery.
  #
  # Cheap by design — a toolchain for `cargo metadata` and `cargo package
  # --no-verify`, but no build. The authoritative check is
  # `cargo publish --workspace --dry-run` in release.yml, which verify-builds
  # every packaged crate; that costs a full rebuild and belongs on tags, not on
  # every PR. Unconditional (no `changes` gate) because it takes seconds, which
  # also keeps it out of ci-success's allowed-skips.
  packaging:
    name: packaging
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Crate embeds stay inside their package
        run: .github/scripts/check-package-embeds.sh

      - name: Deployment manifest mirror in sync
        run: .github/scripts/check-deployment-mirror.sh

      # In a script, not inline: a `run:` block is templated by Actions before
      # bash sees it, so an inline grep for the literal `${{ github.repository }}`
      # would search for the expanded value and never match.
      - name: Image name agrees across workflows and the signing script
        run: .github/scripts/check-image-name.sh

      # The Rust version is written in rust-toolchain.toml, Cargo.toml's
      # rust-version and every dtolnay/rust-toolchain step in this file and
      # release.yml; each is read by a different tool. Dependabot bumps only the
      # action refs, so this is what turns that PR red until the two TOML sites
      # move with it. Runs before the toolchain install on purpose: it needs no
      # cargo, and a drifted pin is the one thing that should fail first.
      - name: Rust version agrees across toolchain, manifest and workflows
        run: .github/scripts/check-toolchain-pin.sh

      # A member that restates `version` or omits `[lints] workspace = true`
      # compiles green and ships its own value. release.yml catches the version
      # half at tag time; this catches both on every PR.
      - name: Every member inherits the workspace version and lints
        run: .github/scripts/check-workspace-manifests.sh

      - uses: dtolnay/rust-toolchain@1.95.0

      # Cargo enforces acyclicity and nothing more. The dependency flow in
      # CLAUDE.md § Crate Structure — three leaves, cli/e2e as sinks, an
      # iroh-blobs-free CLI and client SDK — is held by the table in
      # check_crate_edges.py.
      # Needs `cargo metadata`, hence after the toolchain step.
      - name: Crate dependency direction matches CLAUDE.md
        run: .github/scripts/check-crate-edges.sh

      # The authoritative packaging check is `cargo publish --workspace
      # --dry-run` in release.yml, which verify-BUILDS every crate. This is the
      # cheap half: --no-verify skips the build, so it costs seconds, and it
      # still catches everything about the packaged *manifests* — a crate whose
      # README was deleted, a path dependency that lost its `version`, or a new
      # workspace member that defaults to publishable with no metadata. All of
      # those are otherwise green until release day, when the version is
      # already tagged and a crates.io failure has no clean recovery.
      #
      # Must be --workspace: `cargo package -p decdn-cli` alone fails, because
      # the internal path deps are not on crates.io yet.
      - name: Every crate still packages
        run: cargo package --workspace --no-verify --locked

  # Nothing validated the Dockerfile until a release tag was cut, which is how
  # `EXPOSE 4433 9090  # comment` — a hard parse error, since Dockerfile `#`
  # only starts a comment at the beginning of a line — survived in the tree.
  # The release workflow assembles the image from prebuilt archives; here a
  # placeholder stands in for the binary so the build stays cheap.
  docker-build:
    name: docker build
    runs-on: ubuntu-latest
    needs: changes
    if: ${{ needs.changes.outputs.docker == 'true' || github.event_name != 'pull_request' }}
    steps:
      - uses: actions/checkout@v7

      - name: Lint Dockerfile
        run: docker buildx build --check .

      - name: Build (single platform, placeholder binary)
        run: |
          set -euo pipefail
          mkdir -p dist/amd64
          printf '#!/bin/sh\necho placeholder\n' > dist/amd64/decdn-node
          chmod +x dist/amd64/decdn-node
          docker build --platform linux/amd64 -t decdn-node:ci .

      # Guards the contract the release workflow depends on: the binary must
      # land on PATH at the expected name and be executable as ENTRYPOINT.
      - name: Check the entrypoint resolves
        run: |
          set -euo pipefail
          out=$(docker run --rm decdn-node:ci)
          [ "$out" = "placeholder" ] || {
            echo "::error::entrypoint did not run the copied binary (got: $out)"; exit 1; }

  # Single gate for branch protection. Point required-checks at this one job
  # so adding/renaming jobs above doesn't require updating branch rules.
  ci-success:
    name: CI success
    if: always()
    runs-on: ubuntu-latest
    needs: [fmt, clippy, changes, test, doctest, deny, doc, public-api, markdownlint, adr-book-list,
            pr-title, anvil-e2e-settlement, anvil-e2e-journeys,
            solidity-fmt, solidity-build-test, solidity-coverage,
            solidity-solhint, solidity-slither, solidity-aderyn, ed25519-vectors,
            docker-build, packaging]
    steps:
      - uses: re-actors/alls-green@release/v1
        with:
          jobs: ${{ toJSON(needs) }}
          # Rust jobs are gated on `needs.changes.outputs.rust`,
          # adr-book-list on `needs.changes.outputs.adr`, markdownlint on
          # `needs.changes.outputs.markdown`, the solidity-* jobs on
          # `needs.changes.outputs.solidity`, and docker-build on
          # `needs.changes.outputs.docker`; each skips when its paths are
          # untouched. alls-green treats `skipped` as failure unless listed
          # here. `changes` and `packaging` are intentionally omitted — both
          # always run, so a skip from either signals a misconfiguration that
          # should fail the gate.
          allowed-skips: fmt, clippy, test, doctest, deny, doc, public-api, adr-book-list,
                         pr-title, markdownlint,
                         anvil-e2e-settlement, anvil-e2e-journeys,
                         solidity-fmt, solidity-build-test, solidity-coverage,
                         solidity-solhint, solidity-slither, solidity-aderyn, ed25519-vectors,
                         docker-build
