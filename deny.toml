[advisories]
version = 2
# Transitive advisories suppressed with justification. The first four are RUSTSEC
# "unmaintained" informational notices (not memory-safety / soundness
# vulnerabilities), each a build-time-only proc-macro or a platform-conditional
# dep with no runtime exposure in our binaries. The last two (quick-xml) are DoS
# vulnerabilities that are unreachable in our shipped binaries because the crate
# is a macOS/iOS-only transitive dep — see their per-entry note. None has a
# reachable patched release. Each entry records the affected crate, why it is safe
# to suppress, and what to watch for so a future audit knows when the ignore can
# be dropped. Status as of 2026-07.
# The `crate -> dep -> dep` chains below are reverse-dependency paths as produced
# by `cargo tree -i`: read left-to-right as "is depended on by" (the leftmost
# crate is the flagged one; each `->` points to a crate that pulls in the prior).
ignore = [
    # proc-macro-error v0.4.x: unmaintained (RustSec: maintainer unreachable, no
    # release in 4 years; suggested replacements manyhow / proc-macro2-diagnostics).
    # Build-time proc-macro only, no runtime code. Transitive via
    # genawaiter-proc-macro -> genawaiter -> bao-tree / iroh-blobs. No fixed release
    # exists. Re-evaluate when iroh-blobs / bao-tree drop their genawaiter dependency.
    "RUSTSEC-2024-0370",
    # atomic-polyfill v1.x: unmaintained (RustSec: repo archived/deprecated; upstream
    # suggests portable-atomic). Platform-conditional dep — not compiled on our
    # default linux x86_64 / aarch64 targets; only appears under `cargo tree --target
    # all` via heapless 0.7 -> postcard. No fixed release exists. Re-evaluate when
    # postcard bumps to a heapless 0.8+ that drops atomic-polyfill.
    "RUSTSEC-2023-0089",
    # paste v1.0.x: unmaintained (RustSec: author archived the repo; suggested
    # replacement pastey). Build-time proc-macro only, no runtime code. Transitive
    # via alloy-primitives -> alloy. No fixed release exists. Re-evaluate when
    # alloy-primitives drops paste (tracked upstream in the alloy-rs/core repo).
    "RUSTSEC-2024-0436",
    # proc-macro-error2 v2.x: unmaintained (RustSec: author confirmed no longer
    # maintained; suggests migrating to manyhow / proc-macro2-diagnostics). This is
    # the formerly-maintained fork of proc-macro-error (itself ignored above as
    # RUSTSEC-2024-0370) and is now unmaintained too. Build-time proc-macro only,
    # no runtime code. Transitive via alloy-sol-macro -> alloy-sol-types -> alloy.
    # No fixed release exists. Re-evaluate when alloy-sol-macro drops it.
    "RUSTSEC-2026-0173",
    # quick-xml v0.38.x: two DoS vulnerabilities in XML parsing — RUSTSEC-2026-0194
    # (O(N^2) duplicate-attribute check, CPU exhaustion) and RUSTSEC-2026-0195
    # (unbounded namespace-declaration allocation in NsReader, OOM). Both need
    # quick-xml >= 0.41.0. quick-xml reaches us ONLY via `plist`, which netdev
    # declares as a macOS/iOS-only dependency (netdev's
    # `[target.'cfg(any(target_os = "macos", target_os = "ios"))'.dependencies.plist]`;
    # chain quick-xml -> plist -> netdev -> netwatch -> iroh), so it is NOT compiled
    # into our Linux/musl deployment binaries; cargo-deny flags it only because it
    # audits the whole cross-platform graph. Even on macOS, netdev feeds plist only
    # local, trusted system network data — never attacker-controlled XML / NsReader
    # input. No reachable fix: the newest plist (1.9) still pulls quick-xml 0.39
    # (< 0.41), and iroh 1.0 / netwatch 0.19 / netdev 0.44 version-pin the chain.
    # Re-evaluate when that chain reaches quick-xml >= 0.41, or if macOS/iOS becomes
    # a shipped target — tracked in #1056.
    "RUSTSEC-2026-0194",
    "RUSTSEC-2026-0195",
    # NOTE: the hickory-proto / hickory-net DNS DoS ignores
    # (RUSTSEC-2026-0119 + 0120) were dropped when iroh 1.0 pulled the
    # fixed hickory 0.26.1 (iroh 0.98 had exact-pinned the vulnerable
    # =0.26.0-beta.4). `cargo deny check advisories` flags either as
    # "advisory-not-detected" if re-added.
]

[licenses]
version = 2
allow = [
    "MIT",
    "Apache-2.0",
    "BSD-2-Clause",
    "BSD-3-Clause",
    "ISC",
    "Unicode-3.0",
    "Unicode-DFS-2016",
    "OpenSSL",
    "Zlib",
    "BSL-1.0",
    "CC0-1.0",
    "MPL-2.0",
    "Unlicense",
    "CDLA-Permissive-2.0",
]

[bans]
multiple-versions = "warn"
wildcards = "warn"

[sources]
unknown-registry = "deny"
unknown-git = "deny"
allow-registry = ["https://github.com/rust-lang/crates.io-index"]
allow-git = []
