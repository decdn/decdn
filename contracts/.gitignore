node_modules/
report.sarif
aderyn-report.md
aderyn-report.json
aderyn-report.sarif
lcov.info
baseline/

# Test-only deploy manifests. DeployProtocolScriptTest writes
# deployments/3133769*.json (test-only chain ids); a failed/interrupted run can
# leave one on disk before _cleanupManifest runs. Real-chain manifests use other
# ids and can still be committed with `git add -f`.
deployments/3133769*.json
